<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4">
  <div class="container-fluid">

    <a class="navbar-brand fw-medium" href="/">
      <i class="bi bi-feather me-2"></i>BlogStack
    </a>

    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">

      <ul class="navbar-nav me-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
            <i class="bi bi-grid me-1"></i>Categorias
          </a>
          <ul class="dropdown-menu dropdown-menu-dark border-0 shadow">
            {{#each categorias}}
              <li>
                <a class="dropdown-item" href="/categorias/{{slug}}">{{nome}}</a>
              </li>
            {{else}}
              <li>
                <span class="dropdown-item text-muted">Nenhuma categoria</span>
              </li>
            {{/each}}
          </ul>
        </li>
      </ul>

      <ul class="navbar-nav align-items-center">
        {{#if user}}
          <li class="nav-item me-3">
            <span class="text-white small fw-medium">
              Bem-vindo, {{primeiroNome user.nome}}
          </span>
          </li>
          {{#if user.admin}}
          <li class="nav-item me-2">
            <a class="nav-link" href="/admin">
              <i class="bi bi-speedometer2 me-1"></i>Painel
            </a>
          </li>
          {{/if}}
          <li class="nav-item">
            <a class="btn btn-outline-light btn-sm px-3" href="/usuario/logout">
              <i class="bi bi-box-arrow-right me-1"></i>Sair
            </a>
          </li>
        {{else}}
          <li class="nav-item">
            <a class="btn btn-primary btn-sm px-3" href="/usuario/login">
              <i class="bi bi-box-arrow-in-right me-1"></i>Entrar
            </a>
          </li>
        {{/if}}
      </ul>

    </div>
  </div>
</nav>