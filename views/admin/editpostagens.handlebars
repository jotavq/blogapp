<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-lg-8">

      <div class="d-flex align-items-center justify-content-between mb-4">
        <div>
          <h2 class="fs-4 fw-medium mb-1">Editar postagem</h2>
          <p class="text-muted small mb-0">Atualize os dados abaixo</p>
        </div>
        <a href="/admin/postagens" class="btn btn-outline-secondary btn-sm">
          <i class="bi bi-arrow-left me-1"></i>Voltar
        </a>
      </div>

      {{#each erros}}
        <div class="alert alert-danger d-flex align-items-center gap-2 py-2 small" role="alert">
          <i class="bi bi-exclamation-circle"></i> {{texto}}
        </div>
      {{/each}}

      <div class="card border shadow-none rounded-3">
        <div class="card-body p-4">
          <form action="/admin/postagens/edit" method="POST">
            <input type="hidden" name="id" value="{{postagem._id}}">

            <div class="mb-3">
              <label for="titulo" class="form-label small fw-medium text-secondary">Título</label>
              <input type="text" id="titulo" name="titulo" class="form-control" value="{{postagem.titulo}}" required>
            </div>

            <div class="mb-3">
              <label for="slug" class="form-label small fw-medium text-secondary">Slug</label>
              <input type="text" id="slug" name="slug" class="form-control" value="{{postagem.slug}}" required>
              <small class="text-muted">Usado na URL. Ex: <code>minha-postagem</code></small>
            </div>

            <div class="mb-3">
              <label for="descricao" class="form-label small fw-medium text-secondary">Descrição</label>
              <input type="text" id="descricao" name="descricao" class="form-control" value="{{postagem.descricao}}" required>
            </div>

            <div class="mb-3">
              <label for="categoria" class="form-label small fw-medium text-secondary">Categoria</label>
              <select id="categoria" name="categoria" class="form-select">
                {{#each categorias}}
                  <option value="{{this._id}}" {{#if (eq this._id ../postagem.categoria._id)}}selected{{/if}}>{{nome}}</option>
                {{else}}
                  <option value="0">Nenhuma categoria registrada</option>
                {{/each}}
              </select>
            </div>

            <div class="mb-4">
              <label for="conteudo" class="form-label small fw-medium text-secondary">Conteúdo</label>
              <textarea id="conteudo" name="conteudo" class="form-control" rows="12">{{postagem.conteudo}}</textarea>
            </div>

            <button class="btn btn-success w-100" type="submit">
              <i class="bi bi-check-circle me-2"></i>Salvar alterações
            </button>

          </form>
        </div>
      </div>

    </div>
  </div>
</div>